/* 推荐的基础配置 */
QWidget {
    background-color: rgba(24, 24, 24, 0.001);
}

QMainWindow, QDialog {
    background-color: rgba(24, 24, 24, 0.001);
}

/* 重置可能覆盖背景的组件 */
QScrollArea, QFrame, QLabel {
    background-color: rgba(24, 24, 24, 0.001);
}