HomeWidget #sourceWidget {
    background: transparent;
}

/* GroupHeaderCardWidget 透明样式 */
GroupHeaderCardWidget {
    background-color: rgba(255, 255, 255, 0.005);
    border-radius: 8px;
}

/* 滚动区域透明 */
SingleDirectionScrollArea {
    background: transparent;
    border: none;
}
